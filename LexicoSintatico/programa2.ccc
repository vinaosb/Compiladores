{
	{
		int i;
		int j;
		int min_idx; 

		for (i = 0; i < n-1; i++) 
		{ 
			min_idx = i; 
			for (j = i+1; j < n; j++) 
			  if (arr[j] < arr[min_idx]) 
				min_idx = j; 
		} 
	} 

	{ 
		int i; 
		for (i=0; i < size; i++) 
			printf(arr[i]); 
		print("\n"); 
	} 
  
	int main() 
	{ 
		int arr[5];
		int i;
		for (i = 0; i < 5; i++)
			arr[i] = 5-i;
		selectionSort(arr, n); 
		print("Sorted array: \n"); 
		printArray(arr, n); 
		return 0; 
	} 
}